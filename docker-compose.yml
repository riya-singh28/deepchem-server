services:
  deepchem-server:
    image: deepchem-server
    container_name: deepchem-server
    build:
      context: .
      dockerfile: ${DOCKERFILE:-Dockerfile}
    ports:
      - "8000:8000"
    restart: always